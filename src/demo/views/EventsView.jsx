import { eventLog } from '../../data/patients'
import { Download, Search } from 'lucide-react'
export function EventsView(){return <><div className="view-heading"><div><span>EVENT HISTORY</span><h1>An accountable local record.</h1><p>System, patient and operator events from this session.</p></div><button className="map-control"><Download/>Export local copy</button></div><div className="event-tools"><label><Search/><input placeholder="Search events"/></label><select><option>All event types</option><option>Priority changes</option><option>System events</option></select><span>6 events</span></div><div className="event-table"><header><span>Time</span><span>Level</span><span>Subject</span><span>Event</span><span>Source</span></header>{eventLog.map((e,i)=><div key={i}><time>{e.time}</time><span className={`event-level event-level--${e.level}`}>{e.level}</span><b>{e.patient}</b><p>{e.event}</p><span>{e.source}</span></div>)}</div></>}
